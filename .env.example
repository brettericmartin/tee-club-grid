# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# API Configuration (for Vercel Edge Functions)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key
OPENAI_API_KEY=your_openai_api_key

# Email Provider Configuration (Resend)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=hello@teed.club
RESEND_FROM_NAME=Teed.club
RESEND_REPLY_TO=support@teed.club

# Email Settings
EMAIL_ENABLED=true                    # Set to false to disable email sending
EMAIL_DEV_MODE=false                  # Set to true to redirect all emails to test recipient
EMAIL_TEST_RECIPIENT=dev@example.com  # Test recipient for development

# Optional: Public URL for email links
VITE_PUBLIC_URL=https://teed.club

# Optional: Scraper API URL (if using equipment scraper)
# VITE_SCRAPER_API_URL=https://your-scraper-api-url

# Note: Never commit actual keys to version control
# In Vercel, add these as environment variables in the project settings
# 
# Required Environment Variables:
# - SUPABASE_SERVICE_KEY: Service role key for server-side operations
# - OPENAI_API_KEY: OpenAI API key for Vision API access
# - RESEND_API_KEY: Resend API key for transactional emails (or use alternative provider)